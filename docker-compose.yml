services:
  mongo:
    image: mongo:7
    container_name: tasheel-mongo
    ports:
      - "27017:27017"
    volumes:
      - tasheel_mongo:/data/db
    restart: unless-stopped

volumes:
  tasheel_mongo:
